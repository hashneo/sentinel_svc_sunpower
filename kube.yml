apiVersion: v1
kind: Service
metadata:
  name: sentinel-sunpower
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 5050
    protocol: TCP
    targetPort: 5000
  selector:
    app: sunpower
    context: sentinel-services
    version: v1
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: sentinel-sunpower
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: sunpower
        context: sentinel-services
        version: v1
    spec:
      containers:
        -
          name: sentinel-sunpower
          image: docker.steventaylor.me/sentinel-sunpower-x86_64:latest
          imagePullPolicy: Always
          env:
            -
              name: "PORT"
              value: "5000"
            -
              name: "CONSUL"
              value: "10.0.1.10"
          ports:
            -
              containerPort: 5000
              protocol: TCP
      nodeSelector:
          beta.kubernetes.io/arch: amd64